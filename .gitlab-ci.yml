image: monachus/hugo

variables:
  GIT_SUBMODULE_STRATEGY: recursive

test:
  script:
  - hugo
  except:
  - master

pages:
  script:
  - hugo version
  - env
  - npm install -g postcss-cli
  - hugo
  artifacts:
    paths:
    - public
  only:
  - master
